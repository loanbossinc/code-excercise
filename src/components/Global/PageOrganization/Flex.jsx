import React from 'react';
import styled from 'styled-components';

// Fan of Flexbox so like having something like this handy. Would add in a Flex component that accepts various customization props but no time for that lol.
export const Flex = styled.div`
  display: flex;
  width: 100%;
  box-sizing: border-box;
`

export default Flex;